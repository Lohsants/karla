 //Armazenar oss dados
 let dados = [];

 // Gerar ID aleatório com 5 dígitos
 function gerarld(){
   return Math.floor(10000 + Math.random() * 90000);
 } 

  //Adicionar dados à tabela
  document.getElementByld('form').addEventListener9 ('submit, function(event) {
    Event.preventDefault









}

